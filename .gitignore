# Dependencies
node_modules/

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vercel
.vercel/

# Env files (this project currently hardcodes the Supabase anon key client-side
# on purpose — that key is safe to expose — but if you ever move secrets into
# an .env file, keep it out of git)
.env
.env.local
.env*.local
